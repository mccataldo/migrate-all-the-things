// require functions
// module export

var getPropertyIds = function () {

  // GET /properties

  // extract ids out of the json response
  // return an array of ids for all the properties
}

var getPropertyById = function (propertyId) {

  // GET /property/id

  // return json response without formatting

}

var getAuditIds = function (propertyId) {

  // GET /property/id/audits

  // extract ids out of the json response
  // return an array of ids for all the audits


}

var getAuditById = function (auditId) {

  // GET /audit/id

  // return json response without formatting
}

var getSimulationIds = function (propertyId) {

  // GET /property/id/simulations

  // extract ids out of the json response
  // return an array of ids for all the simulations

}

var getSimulationById = function (simulationId) {

  // GET /simulation/id

  // return json response without formatting

}
