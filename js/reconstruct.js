// require functions
// module export

var reconstructDomain = function (jsonData) {

  // reconstruct legacy domain (property) json to new once

  // return new domain json data

}

var reconstructAuditActions = function (json) {

  // reconstruct legacy audit json data to new one

  // return new audit action json data

}

var reconstructAudit = function (json) {

  // reconstruct legacy audit json data to new one

  // return new audit json data

}

var reconstructSimulation (json) {

  // reconstructs legacy simulation json data to new one

  // return new simulation json data

}
